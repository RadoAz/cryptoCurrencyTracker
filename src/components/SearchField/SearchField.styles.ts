import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  searchContainer: {
    height: 40,
    borderColor: 'gray',
    borderWidth: 1,
    margin: 10,
    padding: 10,
    borderRadius: 6,
    backgroundColor: '#fff',
  },
});
