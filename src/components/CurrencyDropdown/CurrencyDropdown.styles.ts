import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    width: '100%',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
  },
  picker: {
    height: 50,
    width: '100%',
  },
  valueText: {
    fontSize: 18,
    marginRight: 15,
  },
});
